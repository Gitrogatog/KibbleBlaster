
public enum JumperState
{
    Inactive, Walk, JumpSquat, Jump, Land
}

public enum RadishState
{
    Jump, Land, InitialJump
}

public enum BushState
{
    InitialJump, Run
}

public enum GhostState
{
    Idle, Shoot
}